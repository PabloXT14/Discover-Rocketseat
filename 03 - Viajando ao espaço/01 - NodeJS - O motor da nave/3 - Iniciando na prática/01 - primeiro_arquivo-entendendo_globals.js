/*
    * Para executar um arquivo no NodeJS, basta criar um arquivo .js, e acesar através do terminal no diretorio especificado, e executar atráves da palavra chave 'node'
*/

console.log(global);

/*
  # Variáveis Globais do Node 
    - console (executa algo no terminal)
    - __dirname (retorna o nome do diretorio local do arquivo executado)
    - __filename (retorna caminho completo + nome do arquivo executado)
    * veja sobre variáveis globais em: https://nodejs.org/dist/latest-v14.x/docs/api/globals.html

*/