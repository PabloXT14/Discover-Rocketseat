/* Manipulando String e Numbers */

// length => Contar quantos caracteres tem uma palavra e quantos dígitos tem um número (obs: é preciso converter o number p/ string, pois o length não aceita number)
let word = 'Paralelepipedo'
console.log(word.length)

let number = 1234
console.log(String(number).length);